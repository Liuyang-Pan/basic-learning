package org.example.collection.datastructure;

/**
 * 数据结构-数组
 * 内存中的一块连续区域，具有索引
 * 类似酒店房间一样，连续的房间并且有连续的编号
 * 特点：
 * 1、查询速度快:查询数据通过地址值和索引定位，查询任意数据耗时相同。(元素在内存中是连续存储的)
 * 2、删除效率低:要将原始数据删除，同时后面每个数据前移。
 * 3、添加效率极低:添加位置后的每个数据后移，再添加元素。
 */
public class Array {
}
